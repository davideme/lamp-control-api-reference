# Storage Backend Configuration
# If DATABASE_URL is set, PostgreSQL storage will be used.
# If DATABASE_URL is not set (or commented out), in-memory storage will be used.

# PostgreSQL Connection URL
# Uncomment and set this to enable PostgreSQL storage:
# DATABASE_URL=postgresql+asyncpg://lamp_user:lamp_password@localhost:5432/lamp_control

# Alternative: Individual connection parameters (only used if DATABASE_URL is not set)
# These are fallback defaults if you want to build the URL from parts
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=lamp_control
# DB_USER=lamp_user
# DB_PASSWORD=lamp_password

# Connection Pool Settings (optional)
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
