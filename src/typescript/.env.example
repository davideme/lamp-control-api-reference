# Database Configuration
# NOTE: These credentials are placeholders. Replace with your real database user, password, host, and database name in non-example environments.
DATABASE_URL="postgresql://your_username:your_secure_password@localhost:5432/your_database?schema=public"

# Application Configuration
PORT=8080
NODE_ENV=development

# Repository Selection
# Set to 'true' to use PostgreSQL, leave unset or set to 'false' to use in-memory storage
USE_POSTGRES=false
